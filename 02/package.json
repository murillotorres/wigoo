{
  "name": "vitalis-conversions-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Recebe o webhook de pedido da VTEX e envia a conversao para Google Ads e Meta CAPI, sem duplicar e sem deixar um destino travar o outro.",
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "tsx --test tests/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
